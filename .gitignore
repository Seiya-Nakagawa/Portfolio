# Python
__pycache__/
*.pyc
.venv/

# skillsheet-builder のビルド生成物
tools/skillsheet-builder/output/

# 職務経歴書ローカル作業ディレクトリ（Googleドライブへのシンボリックリンク、実データは非公開）
# 末尾にスラッシュを付けない: シンボリックリンクは trailing slash パターンにマッチしないため
skillsheet

# clasp のローカル接続設定（scriptId 等の環境固有情報のためコミットしない）
.clasp.json

# webapp（Django）のローカル生成物・機密設定
webapp/.env
webapp/staticfiles/
webapp/.ruff_cache/
webapp/db.sqlite3
