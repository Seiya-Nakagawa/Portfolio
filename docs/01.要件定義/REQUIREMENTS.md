# 要件定義書 - Portfolio サイト

## 1. プロジェクト概要

### 1.1. 目的

- 転職・案件獲得のためのアピール用ポートフォリオサイトを提供する
- 保有スキル・保有資格・実績を一覧できる形で公開する

### 1.2. 背景

| 項目 | 内容 |
| ---- | ---- |
| プロジェクト名 | Portfolio サイト |
| 対象サイト | <https://seiya-nakagawa.github.io/Portfolio/> |
| ホスティング | GitHub Pages |

- 旧サイトは Bootstrap 4.5 + jQuery 依存の汎用テンプレートで構成され、デザインの差別化・パフォーマンス・保守性に課題があった
- リニューアルの過程でダークモード/ライトモード切替、日本語/英語の多言語対応、サイト内お問い合わせフォームを一時的に実装したが、
  「個人が月1回程度更新する」という運用実態に対して過剰であったため、現行実装では撤去済みである

### 1.3. 方針

- 静的サイト（HTML/CSS/Vanilla JavaScript）として GitHub Pages でのホスティングを継続する
- ビルドプロセス・外部フレームワークへの依存を持たない
- 表示言語は日本語に固定する（多言語対応は行わない）
- 配色・見た目はダークモード基調に固定する（テーマ切替機能は持たない）

## 2. 対象範囲

### 2.1. スコープ内

- Hero / About / Skills / Certifications / Works / Contact の各セクション表示
- スキル・資格・実績データのメンテナンス（`js/data.js` の編集）
- レスポンシブ対応（モバイル / タブレット / デスクトップ）

### 2.2. スコープ外

- サーバーサイド処理・データベース連携（すべて静的コンテンツとして構成する）
- サイト内でのお問い合わせフォーム処理（Google フォームへ誘導するのみとする）
- 多言語表示、テーマ（ダーク / ライト）切替

### 2.3. 現行機能との差分

該当なし。本書は現行実装を正としてまとめたものであり、旧版との差分管理は行わない。

## 3. 用語定義

該当なし。

## 4. 機能要件

| 機能 | 説明 | 優先度 |
| ---- | ---- | ------ |
| ナビゲーション | アンカーリンクによるスムーズスクロール、モバイル用ハンバーガーメニュー | 高 |
| Hero | タイピングアニメーションによる肩書き表示 | 中 |
| About | プロフィール情報表示、生年月日からの年齢自動計算 | 高 |
| Skills | カテゴリ別のスキルバー表示（技術名・経験年数・習熟度） | 高 |
| Certifications | 取得資格の一覧表示 | 中 |
| Works | 実績カードの一覧表示（GitHub リポジトリ等へのリンク） | 高 |
| Contact | Google フォームへのリンク、GitHub プロフィールへのリンク | 中 |

## 5. 管理対象データ

### 5.1. データに関する業務ルール

- スキル情報: `name` / `years` / `level`（1〜5の5段階） / `category` の組で保持し、`js/data.js` で管理する
- 資格情報: 取得資格名の一覧として `js/data.js` で管理する
- 実績情報: タイトル・説明・リンク等を `js/data.js` で管理する
- 職務経歴書（スキルシート）の実データは、個人情報および NDA 相当の案件情報を含むため、
  本リポジトリでは一切管理しない。ローカル環境で完結させる運用とする（詳細は
  [基本設計書 8章](../02.設計/DESIGN.md#8-職務経歴書の管理方式)を参照）

## 6. 非機能要件

### 6.1. 可用性

- GitHub Pages の可用性に準ずる。個人サイトのため独自の SLA 目標は設けない

### 6.2. 性能

| 項目 | 目標値 |
| ---- | ------ |
| ページ読み込み | 3 秒以内 |
| 外部ライブラリ | 最小限（Font Awesome、Google Fonts のみ） |

### 6.3. セキュリティ

- 個人情報・NDA 相当の案件情報を含む職務経歴書の実データは GitHub にアップロードしない
- 職務経歴書の作業ディレクトリは本リポジトリ内のパス（`skillsheet/`）に配置するが、
  `.gitignore` で除外し GitHub には一切アップロードしない
- `tools/skillsheet-builder/` で公開するビルドスクリプト・テンプレートには、ダミーデータのみを同梱する

### 6.4. バックアップ・リストア

該当なし。静的サイトのソースは GitHub リポジトリの履歴で管理する。

### 6.5. 運用・保守

- 更新頻度は月 1 回程度を想定する
- ビルド・デプロイの自動化（CI）は用いず、`main` ブランチへのマージによって GitHub Pages へ反映する

### 6.6. コスト

- GitHub Pages の無料枠の範囲内で運用する

### 6.7. 移植性・保守性

- フレームワーク非依存の Vanilla 構成とし、特定のビルドツールへの依存を持たない

## 7. 制約・前提条件

### 7.1. 採用が決定済みの技術

- HTML5
- CSS3（Vanilla CSS）
- JavaScript（Vanilla、ES6+）
- GitHub Pages

### 7.2. その他の前提

- 本リポジトリは public で公開する
- 職務経歴書の作業ディレクトリは本リポジトリのパス配下（`skillsheet/`）に配置する。
  実体は Google ドライブ上に置き、`.gitignore` で除外することで
  GitHub には一切アップロードされないようにする
